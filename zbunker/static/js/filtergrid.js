var button = 1; // buttons value ranges from 1 to 5. Each number for a certain filter.
var $container = $('gallery-content-center')

// applying isotope script
$container.isotope({itemSelector : 'img'});
